import {t} from "../i18n/translater.js";

export const price_sold = {
    name: 'price_sold',
    label: t.fields.price.sold,
    hint: t.fields.price.hint,
    widget: 'number',
    required: false,
    i18n: 'duplicate'
}
